ktor {
    deployment {
        environment = development
        port = 8080
    }

    application {
        modules = [ServerKt.main]
    }
}

twitter {
    token = ${TWITTER_TOKEN}
    rules = []
}
